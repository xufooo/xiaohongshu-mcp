name: Build Check

on:
  push:
    branches: [ fixup, fixup-pre, fixup-test ]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: "1.24"
    - name: Verify
      run: |
        go test ./...
        go build ./...
        (cd third_party/headless_browser && go mod tidy && go test ./...)

  build:
    needs: verify
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64]
        include:
          - goos: darwin
            goarch: arm64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: arm64
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: "1.24"
    - name: Build
      run: |
        export GOOS=${{ matrix.goos }}
        export GOARCH=${{ matrix.goarch }}
        export GOFLAGS=-buildvcs=false
        export CGO_ENABLED=0
        EXT=""
        if [ "$GOOS" = "windows" ]; then EXT=".exe"; fi
        mkdir -p dist
        # -trimpath 去掉构建机绝对路径；-s -w 去掉符号表与调试信息。
        # 产物直接拷进树莓派 SD 卡，体积与页缓存占用都要省。
        go build -v -trimpath -ldflags="-s -w" -o "dist/xiaohongshu-mcp-${GOOS}-${GOARCH}${EXT}" .
        ls -l "dist/xiaohongshu-mcp-${GOOS}-${GOARCH}${EXT}"
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: xiaohongshu-mcp-${{ matrix.goos }}-${{ matrix.goarch }}
        path: dist/xiaohongshu-mcp-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.goos == 'windows' && '.exe' || '' }}
        if-no-files-found: error
