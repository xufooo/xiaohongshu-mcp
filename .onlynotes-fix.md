## 任务：新增 onlyNotes 过滤功能

从上游 #776 适配，但保留了 fork 的 DOM 提取兼容性。

### 1. types.go

在 `Feed` struct 定义后面（约第 28 行），`modelTypeNote` 定义前面，新增：

```go
// modelTypeNote 笔记条目的 modelType 取值。
const modelTypeNote = "note"

// onlyNotes 滤掉非笔记条目（直播卡片 live_v2、搜索热词 hot_query 等）。
//
// 保留 DOM 提取的空 modelType：fork 的 DOM 提取路径可能不设 modelType，
// 保留空值让 mergeFeedsByID 从 state 补全或原样保留，避免误删 DOM 笔记。
func onlyNotes(feeds []Feed) []Feed {
	notes := make([]Feed, 0, len(feeds))
	for _, f := range feeds {
		if f.ModelType == "" || f.ModelType == modelTypeNote {
			notes = append(notes, f)
		}
	}
	return notes
}
```

### 2. search.go — collectResults

找到 `collectResults` 函数中 `return result, nil` 的地方，在 return 前加 `result.Feeds = onlyNotes(result.Feeds)`。

具体位置：约 853 行（`return mergeFeedsByID(stateFeeds, domFeeds), nil` 等 5 处返回）。

### 3. search.go — collectSearchFeeds

找到 `collectSearchFeeds` 函数（约 879 行），每个 return 前调 `onlyNotes`。

### 4. feeds.go — collectHomeFeeds

找到 `collectHomeFeeds` 函数（约 61 行），每个 return 前调 `onlyNotes`。

### 约束
- 不改 dom_extract.go、state reader、hrod 层
- 不改 GetFeedsList、Search、SearchFeedsOnly 等业务入口
- 不改 mergeFeedsByID
- 只改 function 不修已有逻辑
- 注释用中文
