只读审查当前未提交的 xiaohongshu/search.go diff；严禁修改文件、运行/安装 Go、执行 git 写操作、操作服务或 cookies。

真实故障：session_search 调用在 MCP 300s 超时，session 随之失效；但 waitForSearchResults 的设计目标是 30s。旧实现把就绪判断与 Date.now deadline 放进 page.Wait(rod.Eval(...))，现场没有在 30s 返回。结果是后续 Go probe/错误处理永远不执行。

唯一逻辑改动：删除该 page.Wait 的 JS 大轮询，改为与同文件 waitForSearchInput 相同模型：Go deadline=searchResultsWaitTimeout；循环执行 page.Err、一次 probeSearchResultsKeyword、searchResultsReady；不就绪则 page.Sleep(300ms)。probe 成功时清空旧 lastErr；超时后保持既有关键词/无结果错误分类。

请审：1) 30秒上界、返回与错误传播；2) 结果就绪语义是否保持；3) Go 编译/API/并发问题；4) P0/P1。输出 PASS 或 ISSUE（P0/P1、文件:行、阻断原因），最后总体 PASS / FAIL。
